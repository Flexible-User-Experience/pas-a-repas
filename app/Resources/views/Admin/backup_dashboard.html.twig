{% extends 'Admin/base_admin.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>{{ 'frontend.admin.dashboard' |trans |upper }}</h1>
                <h3>{{ 'frontend.admin.welcome' |trans |capitalize }}</h3>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
            <h4>Aquest mes, s'han registrat</h4>
                <div class="panel panel-default">
                    <div class="panel-heading">{{ 'frontend.admin.contact' | trans | upper }}</div>
                        <div class="panel-body">{{ contactMsgsAmount }}</div>
                    <div class="panel-heading">{{ 'frontend.admin.post' | trans | upper }}</div>
                        <div class="panel-body">{{ postsAmount }}</div>
                    <div class="panel-heading">{{ 'frontend.admin.category' | trans | upper }}</div>
                        <div class="panel-body">{{ categoriesAmount }}</div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

