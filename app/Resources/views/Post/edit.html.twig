{% extends 'base.html.twig' %}

{% block main -%}
    <div class="container">
        <div class="row">
            <div class="col-md-12">

                <h1>Post edit</h1>

                {{ form(edit_form) }}

                <ul class="record_actions">
                    <li>
                        <a href="{{ path('admin_post') }}">
                            Back to the list
                        </a>
                    </li>
                    <li>{{ form(delete_form) }}</li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
